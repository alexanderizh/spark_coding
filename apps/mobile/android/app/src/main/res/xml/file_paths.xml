<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Allow FileProvider to serve files from the app's cache directory -->
    <cache-path name="cache" path="." />
    <!-- External cache directory for downloaded APKs -->
    <external-cache-path name="external_cache" path="." />
</paths>
