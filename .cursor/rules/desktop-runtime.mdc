---
description: Desktop 版本、托盘与后台进程控制规范
globs: apps/desktop/**/*
alwaysApply: false
---

# Desktop 运行时规范

## 版本显示

- 版本读取优先使用 desktop 包自身 `package.json`，不要直接依赖 `app.getVersion()` 作为唯一来源
- 版本展示与设备状态上报应复用同一版本获取逻辑，避免不同页面显示不一致

## Windows 托盘图标

- Windows 托盘图标需使用适配尺寸与可见区域裁剪，避免 512 画布直接缩放导致图标近似透明
- 托盘图标创建后需显式关闭模板图标模式，防止 Windows 下渲染异常

## 后台进程控制

- 设置页必须提供显式“重启程序/关闭程序”入口，便于安装更新前释放后台进程
- 关闭程序应先设置 quitting 标记，再执行 `app.quit()`，避免被“关闭窗口即隐藏托盘”逻辑拦截
