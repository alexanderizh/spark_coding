---
description: Mobile 端 UX 与页面生命周期规范
globs: apps/mobile/**/*
alwaysApply: false
---

# Mobile UX 规范

## 列表页返回时自动刷新

- 会话列表、连接列表等数据列表页，在用户从子页面（终端、扫码、设置等）返回时，应自动刷新数据
- 实现方式：使用 `RouteObserver` + `RouteAware`，在 `didPopNext` 中调用刷新逻辑
- 避免用户每次进入列表页都需手动下拉刷新才能看到最新数据
