---
description: Mobile 端 UX 与页面生命周期规范
globs: apps/mobile/**/*
alwaysApply: false
---

# Mobile UX 规范

## 列表页返回时自动刷新

- 会话列表、连接列表等数据列表页，在用户从子页面（终端、扫码、设置等）返回时，应自动刷新数据
- 实现方式：使用 `RouteObserver` + `RouteAware`，在 `didPopNext` 中调用刷新逻辑
- 避免用户每次进入列表页都需手动下拉刷新才能看到最新数据

## 返回列表页时应用级重置（重启式）

- 若出现“从会话详情返回列表后再次进入详情无法连接，但杀进程重启后恢复”的问题，优先使用应用级重置来模拟冷启动
- 推荐实现：用可重建的 `ProviderScope`（切换 `key`）重建整个 Provider 容器，使 Socket/Session/Terminal 等单例服务完整 dispose + 重新创建
- 触发时机：列表页通过 `RouteObserver` + `RouteAware.didPopNext` 监听返回事件后触发重置

## 会话删除后的本地清理

- 刷新连接列表时，若服务端会话列表已不存在某条本地连接，应直接移除本地记录，不仅仅标记离线
- 兼容兜底查询：调用 `GET /api/session/:token` 返回 404/410 时，视为服务端已删除并清理本地缓存
- 网络异常或服务不可达时，不做强删，仅保留本地记录并展示离线状态

## 会话列表系统类型展示

- 会话卡片展示两端 ID 时，需同步展示两端系统类型（desktop 与 mobile），并对 `darwin/win32/ios/android` 做可读化映射。
- mobile 端在 `mobile:join` 时需上报本机系统类型，并在本地连接模型中持久化 `desktopPlatform/mobilePlatform`，确保重启后仍可展示。
- 会话列表数据源必须是云端接口返回，不读取本地会话缓存；本地仅可保留设备 ID 和最后一次服务地址用于发起云端查询。

## 会话级断开与重置

- 全局设置页不提供“断开并重置所有会话/连接”的危险操作，避免用户误触导致所有配对记录被清空
- “断开/重置”应放在单个会话的设置页内，且默认仅影响当前会话：断开只断开连接；重置仅删除本会话配对记录

## 在线会话可删除

- 在线/离线会话都应支持删除配对记录，交互尽量一致（例如都支持右滑删除并二次确认），避免用户误以为在线会话不可删除

## 终端输入栏命令与快捷键维护

- 命令快捷弹窗新增能力时，优先在 `InputToolbar._commands` 统一维护，保持“展示文案 + slash 命令 + 说明”一一对应，避免散落在多个页面。
- Claude 模式切换命令至少包含 `/plan`，并可同时提供 `/code`、`/ask`、`/architect`、`/chat`，统一通过现有发送链路透传到终端。
- 输入栏快捷键新增组合键时，需在 `_keyShortcuts` 中声明可读标签与原始序列，并复用 `onRawInput` 发送，避免新增分支逻辑。
- 终端输入栏 UI 修改后，需执行 `flutter analyze lib/widgets/input_toolbar.dart` 并修复提示（例如可加 `const` 的构造器）后再提交。
