---
description: MidwayJS 服务端启动与配置规范
globs: apps/server/**/*
alwaysApply: false
---

# MidwayJS 服务端规范

## 启动入口

- 使用 `@midwayjs/bootstrap` 的 `Bootstrap.run()`，不要从 `@midwayjs/core` 导入 Bootstrap（core 不导出 Bootstrap）
- 需同时依赖 `@midwayjs/bootstrap` 和 `@midwayjs/logger`

## 配置要求

- `config.keys` 为必填项（Koa cookie 签名），可在 `config.default.ts` 中配置：
  ```ts
  keys: process.env.APP_KEYS?.split(',') ?? ['spark-coder-default-key']
  ```

## Socket.IO 控制器

- 断开连接装饰器为 `OnWSDisConnection`，不是 `OnWSDisconnect`
- `Application` 类型即 Socket.IO Server，使用 `this.app.to(room).emit()`，不要用 `this.app.io.to()`
- 多框架（如 Koa + SocketIO）时，需用 `@App('socketIO')` 注入 Socket.IO 实例，否则 `@App()` 会注入主框架（Koa）的 Application
- `@OnWSMessage` 只接收客户端发送的 payload，不接收 socket 参数；需通过 `@Inject('socket') ctx: Context` 获取当前连接，`Context` 即 socket 实例
- `runtime:ensure`（mobile→agent）和 `runtime:status`（agent→mobile）需要做角色校验并按 `sessionId` 房间中继，agent 不在线时返回 `session:error`
- 断连状态必须与角色一致：agent 断连写 `AGENT_DISCONNECTED`，mobile 断连写 `MOBILE_DISCONNECTED`，避免前后端状态误判
- `mobile:join` 不应在 agent 未连接时直接报错中断；应允许进入 `WAITING_FOR_AGENT`，待 agent 注册后自动补发 `session:pair`
- `agent:register` 若检测到 `mobileSocketId` 已存在，应直接升级 `PAIRED` 并广播 `session:pair`，避免“双方在线但一直等待配对”

## 中继通信日志

- `relay.controller.ts` 在各事件处理处使用 `this.log()` 打印 `[relay]` 前缀日志，便于排查主机端、客户端、服务端之间的通信问题
- `terminal:output` 因频率高，仅每 50 条打印一次

## TypeORM + better-sqlite3

- 实体列需显式指定 `type`，避免 "Data type Object not supported" 错误
- 字符串列：`type: 'varchar'`
- 日期列：`type: 'datetime'`

## 会话状态接口

- `GET /api/session/:token` 若用于 mobile 连接列表展示，需返回 `agentHostname` 字段，卡片主标题优先使用该值。
- 会话列表需要展示两端系统类型时，`GET /api/session/:token` 与 `GET /api/sessions` 均需返回 `agentPlatform` 与 `mobilePlatform` 字段。
- `mobile:join` 需接收并写入 `mobilePlatform`（如 iOS/Android），`session:pair` 需携带两端平台字段，避免桌面端本地记录缺失系统类型。
- 会话列表以云端数据库为准时，服务端需提供按 mobile 与按 desktop 的会话查询接口，客户端不得以本地缓存作为列表真值来源。

## 会话删除一致性

- 客户端刷新时应以数据库会话存在性为准：当接口返回 404/410（不存在或已过期）时，客户端应清理本地配对记录
- `session:deleted` 属于实时同步优化，不能作为唯一清理手段，客户端需具备刷新时的兜底收敛逻辑

## TypeScript 严格模式

- 依赖注入属性（@Inject、@App 等）在 `strictPropertyInitialization` 下需初始化
- 可在 `apps/server/tsconfig.json` 中设置 `strictPropertyInitialization: false` 以兼容 Midway 注入
