---
description: Terminal PTY 与 Claude CLI 启动规范
globs: apps/terminal/**/*
alwaysApply: false
---

# Terminal PTY 规范

## node-pty 与 posix_spawnp 失败

在 macOS 上 node-pty 报 `posix_spawnp failed`，最常见原因是：

### 根本原因：spawn-helper 缺少执行权限

yarn/npm 安装 node-pty prebuilt 时，`spawn-helper` 二进制的执行权限（`+x`）会丢失。
node-pty 所有 spawn 操作都依赖该 helper，一旦它不可执行，**任何 pty.spawn() 都会失败**，与目标程序的路径无关。

**自动修复**：根目录 `postinstall` 已包含 `scripts/fix-node-pty-permissions.js`，`yarn install` 时会自动为当前平台修复权限，用户无需手动操作。

若自动修复失败（如无写权限），可手动执行：
```bash
chmod +x node_modules/node-pty/prebuilds/darwin-arm64/spawn-helper
# 如果是 x64 机器：
chmod +x node_modules/node-pty/prebuilds/darwin-x64/spawn-helper
```

### 路径问题

node-pty 的 posix_spawnp 需要绝对路径，不会自动搜索 PATH。
已通过 `resolveExecutablePath()` 分平台解析：
- **Unix/macOS**：使用 `which`，如 `claude` → `/Users/zhangyang/.local/bin/claude`
- **Windows**：使用 `where`，如 `claude` → `C:\...\claude.exe`

参考：node-pty issues #670、#789。

## 配置

- 开发：`apps/terminal/.env`（复制 `.env.example`）
- 打包/生产：`apps/terminal/.prod.env`（复制 `.prod.env.example`）

CLI 参数 `--server-url` 可临时覆盖。

## 命令

打包发布到 npm 后，用户通过 `npm install -g @spark_coder/terminal` 安装，可直接用 `spark` 命令启动。

## 模块解析

若 `yarn dev:terminal` 报 `Cannot find module '@spark_coder/shared'`，先执行 `yarn install`（postinstall 会创建符号链接）；若仍失败，可执行 `rm -rf node_modules yarn.lock && yarn install` 重建。

## Claude 运行态保障

- 进入会话时由 mobile 发送 `runtime:ensure`，terminal agent 必须幂等处理。
- `claude` 已在运行：回传 `runtime:status`（`ready=true, started=false`）。
- `claude` 未运行：尝试 `pty.spawn()`，成功回传 `ready=true, started=true`。
- 启动失败：回传 `ready=false` 与错误 `message`，不要静默吞错。

## 包装器输出约定

- terminal agent 作为 Claude 包装器时，PTY 输出需同时透传到本地 stdout 与远端 `terminal:output` 事件。
- 连接状态需在本地输出，至少覆盖 `connected / reconnected / disconnected / session:*`。
- Claude 启动状态需在本地输出，至少覆盖 `starting / started / already_running / failed`。
- Claude 输入需输出可观测日志（字节数 + 转义预览），避免仅保留黑盒转发。

## 打包后 Claude 进程立即退出（exit code 1）— PATH 问题

### 现象
打包后的 Electron desktop app 启动时日志出现：
```
Attempting to spawn Claude at: %s claude   ← 路径未解析
Claude process spawned successfully
Claude process exited with code: 1         ← 立即退出
```

### 根本原因
macOS 从 Dock/Finder 启动 `.app` 时，Electron 的 `process.env.PATH` **不继承用户 shell 的完整 PATH**（nvm、Homebrew 等路径均缺失）。

导致问题：
1. `resolveExecutable('claude')` 调用 `which claude` 失败（nvm 路径不在 PATH），回退为原始字符串 `'claude'`
2. claude CLI 是 npm 生成的 shebang 脚本（`#!/usr/bin/env node`），系统找到脚本但 `env node` 失败（node 在 nvm 路径下，不在 PATH 里）
3. 进程立即退出 code 1

### 修复
在 `apps/desktop/src/main/index.ts` 启动时：

1. **用交互式 shell 取 PATH**：`$SHELL -i -c 'echo $PATH'`（`-i` 会加载 `.zshrc`/`.bashrc`，login shell `-lc` 在 zsh 下不加载 `.zshrc`，nvm 会取不到）。
2. **显式把 nvm 的 node bin 目录 prepend 到 PATH**：枚举 `~/.nvm/versions/node/*/bin` 并拼到 PATH 前面，保证 `#!/usr/bin/env node` 一定能找到 `node`，避免 exit 127。

若只做 (1) 而用 `-lc`，macOS zsh 下会拿到不含 nvm 的 PATH，仍会报 `env: node: No such file or directory`。

### claude-detector.ts 补充 nvm 路径
`detectClaudePath()` 须在 `which` 失败后枚举 `~/.nvm/versions/node/*/bin/claude` 作为候选路径（使用 `readdirSync` + `existsSync`）。

## 启动前环境预检

- terminal 启动时先做环境预检，再创建会话。
- 预检必须至少包含：Claude CLI 可用性、服务端可访问性。
- 预检结果必须以中文输出，并显式标识成功/失败。
- 预检失败要给出可执行的修复提示（安装 CLI 或启动 server），并终止启动流程。
